import React from 'react'
import square from './square'

function Board({squares,click}) {
    return (
        <div>
          {squares.map(()=>{
              
          })}  
        </div>
    )
}

export default Board
